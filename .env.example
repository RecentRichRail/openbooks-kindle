# SMTP Configuration for OpenBooks
# Set SMTP_ENABLED=true to enable email functionality

# SMTP Server Settings
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=your_email@gmail.com

# Example configurations for different providers:

# Gmail (recommended: use App Password instead of regular password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# iCloud Mail (recommended: use App Password)
# SMTP_HOST=smtp.mail.me.com
# SMTP_PORT=587

# Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# Yahoo
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# Custom SMTP Server
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587

# Other OpenBooks Settings (optional)
# BASE_PATH=/
# LOG_LEVEL=info
